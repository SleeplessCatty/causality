interface:
  display_name: 'Causality Capture'
  short_description: '将当前对话主线整理为 Causality 入库方案'
  default_prompt: '使用 $causality-capture 采集当前会话主线，生成入库方案，并仅在我明确确认后提交。'

policy:
  allow_implicit_invocation: false
